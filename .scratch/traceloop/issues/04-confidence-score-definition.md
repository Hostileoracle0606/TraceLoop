# 04 — What does the confidence score actually measure?

Type: grilling
Status: resolved (dropped — out of scope)
Blocked by: 02

## Question

The spec promised a confidence score on the causal chain, labelled as deterministic analysis. Decide precisely what it quantifies so it isn't hand-wavy in front of a founder who will probe it.

## Answer

**Dropped from scope (2026-07-17).** The causal chain + plain-language root-cause explanation stand on their own for the demo; a confidence score opens a definitional rabbit hole a founder could probe without adding demo value. The reused dashboard's confidence chip stays as static UI (baked into the bundle). Revisit only if the destination is redrawn.
