# 02 — Adopt the TraceLoop dashboard, render from a static view-model fixture

**What to build:** The existing [TraceLoop dashboard](https://traceloop-firmware-debugger.tsgoswam.chatgpt.site) source is dropped into the repo as the shipping frontend and made **data-driven**: the three views (timeline / board / causal graph) + root-cause narrative + Generate-patch render from a single static **run view-model** JSON fixture (the mockup's RUN-1042 data, promoted from inline hardcoding to a file). This is the frontend half of the seam; it runs in parallel with the engine (ticket 01) and they meet at the view-model schema.

**Blocked by:** None — can start immediately. Requires the mockup source placed in the repo (export the chatgpt.site artifact bundle).

**Status:** done (evidence-panel prose wiring carried to ticket 03)

- [x] The mockup source runs as the app in the repo — lean Vite app in `frontend/` (no vinext/Cloudflare machinery), real 80KB CSS from the bundle, dev server boots and renders.
- [x] The three views render from **one external run view-model JSON fixture** (`frontend/src/run-fixture.json`, generated by the engine's `toDashboardRun()`), not inline-hardcoded data. The big evidence-panel prose (`<h2>` + explanation paragraph) is still static — wired live in ticket 03.
- [x] Observed / Derived / Violated coloring renders from the fixture `kind`; the confidence chip stays static (score dropped from scope, `traceloop/04`).
- [x] Swapping the fixture JSON changes what is drawn — verified: flipping `run.id`/`commit` in the fixture updated the header live (`RUN-SWAP-9999`), and the causal-graph e4 label now reads the fixture's "GPIO pin 13 written" (was hardcoded "main.c:37 wrote pin 13").
- [x] Chrome (top nav, runs list, filter rail) remains static/faked.
