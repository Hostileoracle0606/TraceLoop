# 14 — Notifications & metrics from real data (Task D3)

**What to build:** Derive notifications from `activity_logs` (via `tasks.getActivityLog`), grouped by task, summarised as "1 decision needed"; confirm dashboard metrics use `/api/metrics` with a skeleton and a "No runs yet" empty state.

**Blocked by:** None — can start immediately.

**Status:** complete

- [x] Notifications reflect real activity logs, grouped, decision-first.
- [x] Zero-state shows "No runs yet," never invented numbers.

Full contract: plan → Workstream D, Task D3.
